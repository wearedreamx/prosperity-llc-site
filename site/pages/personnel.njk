---
pagination:
  data: collections.personnel
  size: 1
  alias: person
permalink: "/personnel/{{ person.data.slug }}/"
eleventyComputed:
  title: "{{ person.data.name }} - Prosperity Partners"
searchType: personnel
layout: base.njk
---
<section class="page-banner" data-pagefind-ignore>
	<div class="page-banner-content">
		<div class="page-banner-container container">
			<h2 class="page-banner-title"><a class="page-banner-title-link" href="/meet-the-team/">Meet the Team</a></h2>
		</div>
	</div>
</section>
<section class="single-post-container">
	<div class="single-post-content-container container">
		{#- Same placeholder as the directory cards — see meet-the-team.njk. -#}
		<figure class="single-post-figure"><img width="550" height="500" src="{{ person.data.photo or '/assets/img/ui/team-placeholder.svg' }}" alt="" class="single-post-image" data-pagefind-meta="image[src]"></figure>
		<div class="single-post-content">
			<h1 class="single-personnel-name">{{ person.data.name }}{% if person.data.certifications %}<span class="single-personnel-certs">, {{ person.data.certifications }}</span>{% endif %}</h1>
			{%- if person.data.job_title %}<p class="single-personnel-title">{{ person.data.job_title }}</p>{% endif %}
			{%- if person.data.location_name %}<p class="single-personnel-location"><a href="{{ person.data.location_url }}" class="single-personnel-location-link">{{ person.data.location_name }}</a></p>{% endif %}
			<div class="single-personnel-content content">
				{#- The LinkedIn badge was an icon link with no accessible name on the
				    old site (see reports/prosperity-partners-accessibility-audit.md),
				    repeated across ~200 pages. The alt text names the person so the
				    link reads as a destination rather than as "LinkedIn" 200 times. -#}
				{% if person.data.linkedin_url %}<p><a href="{{ person.data.linkedin_url }}" target="_blank" rel="noopener"><img src="/assets/img/linkedin.png" alt="{{ person.data.name }} on LinkedIn" width="24" height="24"></a></p>{% endif %}
				{{ person.content | safe }}
			</div>
		</div>
	</div>
</section>
